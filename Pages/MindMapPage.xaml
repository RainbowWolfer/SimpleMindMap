<Page x:Class="MindMap.Pages.MindMapPage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	  xmlns:local="clr-namespace:MindMap.Pages"
	  mc:Ignorable="d" 
	  d:DesignHeight="450" d:DesignWidth="800"
	  d:Background="Gray"
	  Title="MindMapPage">

	<Page.Resources>
		<Style x:Key="MenuButton" TargetType="Button">
			<Setter Property="Margin" Value="10,2,10,2"/>
			<Setter Property="Height" Value="25"/>
		</Style>
	</Page.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="170"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<StackPanel>
			<TextBlock Text="This is Tool Menu" Margin="10" FontSize="18"/>
			<Button x:Name="AddRectableButton" Content="Add Rectangle" Click="AddRectableButton_Click" Style="{StaticResource MenuButton}"/>
			<Button x:Name="AddEllipseButton" Content="Add Ellipse" Click="AddEllipseButton_Click" Style="{StaticResource MenuButton}"/>
			<Button x:Name="DebugButton" Content="Debug" Click="DebugButton_Click" Style="{StaticResource MenuButton}"/>
			<TabControl>
				<TabItem Header="1">
					<Grid>
						<TextBlock Text="1"/>
					</Grid>
				</TabItem>
				<TabItem Header="2">
					<Grid>
						<TextBlock Text="2"/>
					</Grid>
				</TabItem>
				<TabItem Header="3">
					<Grid>
						<TextBlock Text="3"/>
					</Grid>
				</TabItem>
			</TabControl>
		</StackPanel>
		<Border Grid.Column="1" BorderBrush="Gray" BorderThickness="2,0,0,0" ClipToBounds="True">
			<Grid>
				<!--Only for detecting contacting canvas through mouse-->
				<Rectangle x:Name="BackgroundRectangle" Fill="Transparent" MouseDown="BackgroundRectangle_MouseDown" MouseMove="BackgroundRectangle_MouseMove" MouseUp="BackgroundRectangle_MouseUp"/>
				<Canvas x:Name="MainCanvas" Loaded="MainCanvas_Loaded">
					<Canvas x:Name="BackgroundCanvas"/>
					<Canvas.RenderTransform>
						<TransformGroup>
							<TranslateTransform x:Name="MainCanvas_TranslateTransform" X="0" Y="0"/>
							<!--Need further working-->
							<ScaleTransform ScaleX="1" ScaleY="1"/>
						</TransformGroup>
					</Canvas.RenderTransform>
				</Canvas>
			</Grid>
		</Border>
	</Grid>
</Page>
